# NBO 自媒体工作台长期维护规则

## 产品边界

- `apps-script/Code.gs` 与 `apps-script/Index.html` 是已冻结的智能文案服务。除非用户明确要求迭代文案服务，否则不得修改。
- 封面制作是独立配套服务：
  - Sites 版本：`app/cover/`
  - 国内稳定静态版本：`docs/cover.html`、`docs/cover.css`、`docs/cover.js`
- 两个封面版本的核心能力必须保持一致；不能只更新其中一个。

## 五年可维护原则

- 平台尺寸、模板名称与规范版本使用独立配置，不把易变化的平台规则散落在页面逻辑中。
- 封面制作的上传、预览和导出必须在浏览器本机完成；不能依赖 AI 服务才能运行。
- 用户照片不得上传、保存或用于训练。
- 所有错误提示必须直接显示中文。
- 每次发布前保留可回退版本；已确认版本不得覆盖或删除。

## 南铂封面硬规则

- 抖音工作封面：1080×1920；居中 3:4 安全区为 y=240..1680。
- 人物原片不拉伸，不重绘脸、五官、发型、皮肤、手、动作和服装。
- 上行文字固定 `#FFFFFF`；下行文字固定 `#FEE800`。
- 文字和品牌位于渐变压暗层上方，压暗层位于人物照片上方。
- 导出图不显示安全区辅助线。
- 封面文案不得把文件名、客户备注或 AI 猜测当成图片事实。

## 验证

- 构建、自动检查与代码规范必须全部通过。
- GitHub Pages 静态入口必须能够单独打开封面制作页。
- 至少验证 JPG 与 PNG 导出代码路径、9:16 与 3:4 尺寸预设、智能文案入口返回路径。
